const assert = require('chai').assert

describe('Status', () => {
	before('setup', done => done())

	describe('#index', () => {
		it('should answer with a random string when the api is up and running')
	})
})